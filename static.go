package main

const VERSION string = "30.0"

var Conf = `[database]
# host is required (network host)
# host =
# port = 3306
# database is required
# database =
# user defaults to OS user
# user =
# password =
`
